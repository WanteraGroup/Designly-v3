name: DESIGNLY Dependency Review

on:
  pull_request:
    branches: [main, fix/provider-auth]

permissions:
  contents: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: Dependency review
        uses: actions/dependency-review-action@v4
